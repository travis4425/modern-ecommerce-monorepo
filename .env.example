# ─────────────────────────────────────────────────────────────
# Biến dùng cho docker-compose (Postgres + pgAdmin) — chạy ở gốc repo
# Copy file này thành .env rồi chỉnh nếu cần
# ─────────────────────────────────────────────────────────────
POSTGRES_USER=ecom
POSTGRES_PASSWORD=ecom_dev_password
POSTGRES_DB=ecommerce
POSTGRES_PORT=5432

PGADMIN_EMAIL=admin@ecom.local
PGADMIN_PASSWORD=admin123   # pgAdmin yêu cầu tối thiểu 6 ký tự
PGADMIN_PORT=5050
